
  # Web de Minijuegos Retro

  This is a code bundle for Web de Minijuegos Retro.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  